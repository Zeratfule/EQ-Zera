[package]
name = "fold"
description = "The module fold: eqlog events in, per-module published snapshots out (JOS-459 phase 2)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
eqlog = { path = "../eqlog" }
serde.workspace = true
serde_json.workspace = true
regex = "1"

[features]
# Phase timers on the combat fold, read by the parity tool's stage report. Off in every build
# that ships: the timers cost, and nothing in the app reads them.
profile = []
